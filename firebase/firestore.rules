rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TrimSwipe accesses reminder registrations only through authenticated
    // callable functions. The Admin SDK used by Functions bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
