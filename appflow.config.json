{
  "apps": [
    {
      "appId": "e5189595",
      "root": ".",
      "preBuild": "mkdir -p $HOME/bin && printf '#!/bin/bash\\nexec /Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild \"$@\" CODE_SIGNING_ALLOWED=NO\\n' > $HOME/bin/xcodebuild && chmod +x $HOME/bin/xcodebuild && export PATH=$HOME/bin:$PATH",
      "builds": {
        "ios": {
          "buildFlags": "CODE_SIGNING_ALLOWED=NO"
        }
      }
    }
  ]
}